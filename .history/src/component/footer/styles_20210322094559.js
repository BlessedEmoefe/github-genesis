import styled from "styled-components";
im[port {color} fro '../../config/colors']

export const FooterStyles = styled.div`
  background: ${color.};
  width: 100%;
  height: max-content;
  margin: 0;
`;
